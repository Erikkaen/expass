package Spring.Assignment;

import Spring.pollapp.model.Poll;
import Spring.pollapp.model.PollManager;
import Spring.pollapp.model.User;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class AssignmentApplicationTests {

  /*
    private PollManager manager;


    @Test
    void setupSampleData() {
        User user1 = manager.createUser("Alice");
        Poll poll = manager.createPoll("Best color?", user1);
        manager.createOption(poll, "Red");
        manager.createOption(poll, "Blue");
    }

    Chose the http way
   */
}
